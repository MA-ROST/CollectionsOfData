#ifndef VECTOR_H 
#define VECTOR_H 

#include <string>

std::vector<float> addToTail(const float input, std::vector<float> vector);
void breakUpString(const std::string input, std::vector<float> vector);
void printVector(std::vector<float> vector);
float getAverageVector(std::vector<float> vector);


#endif
